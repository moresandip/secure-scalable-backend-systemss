{
  "name": "secure-scalable-backend-systems",
  "version": "1.0.0",
  "description": "Secure, Scalable Full-Stack Application with Authentication",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
